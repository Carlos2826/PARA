import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel3DeslizamientoCOPage } from './nivel3-deslizamiento-c.o.page';

describe('Nivel3DeslizamientoCOPage', () => {
  let component: Nivel3DeslizamientoCOPage;
  let fixture: ComponentFixture<Nivel3DeslizamientoCOPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel3DeslizamientoCOPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
