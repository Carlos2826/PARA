import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel2DeslizamientoCOPage } from './nivel2-deslizamiento-c.o.page';

describe('Nivel2DeslizamientoCOPage', () => {
  let component: Nivel2DeslizamientoCOPage;
  let fixture: ComponentFixture<Nivel2DeslizamientoCOPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel2DeslizamientoCOPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
