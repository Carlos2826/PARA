import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel1InundacionCOPage } from './nivel1-inundacion-c.o.page';

describe('Nivel1InundacionCOPage', () => {
  let component: Nivel1InundacionCOPage;
  let fixture: ComponentFixture<Nivel1InundacionCOPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel1InundacionCOPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
