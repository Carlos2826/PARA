import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel2InundacionCOPage } from './nivel2-inundacion-c.o.page';

describe('Nivel2InundacionCOPage', () => {
  let component: Nivel2InundacionCOPage;
  let fixture: ComponentFixture<Nivel2InundacionCOPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel2InundacionCOPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
