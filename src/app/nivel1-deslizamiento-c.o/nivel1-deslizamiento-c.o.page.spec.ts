import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel1DeslizamientoCOPage } from './nivel1-deslizamiento-c.o.page';

describe('Nivel1DeslizamientoCOPage', () => {
  let component: Nivel1DeslizamientoCOPage;
  let fixture: ComponentFixture<Nivel1DeslizamientoCOPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel1DeslizamientoCOPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
